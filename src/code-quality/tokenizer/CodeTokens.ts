export enum CodeTokens {
  // functions
  FUNC = 'FUNCTION',
  FUNCPARAM = 'FUNCPARAM',
  // loops
  FOR = 'FOR',
  WHILE = 'WHILE',
  // conditions
  IF = 'IF',
  ELSE = 'ELSE',
  ELSEIF = 'ELSEIF',
  CASE = 'CASE',
  OR = 'OR',
  AND = 'AND',
  // common line
  LINE = 'LINE',
  COMMENT = 'COMMENT',
}
